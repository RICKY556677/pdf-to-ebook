# pdf-to-ebook

将英文 PDF 翻译为中文并生成精致排版 PDF 电子书的自动化工作流。

## 概述

这是一个完整的自动化流程，能够：

1. **提取**英文 PDF 的文本内容（支持多种引擎，自动降级）
2. **翻译**为中文（保留专业术语）
3. **排版**生成精美的 A4 尺寸电子书（含封面、目录、章节页）
4. **导出**最终 PDF 文件

## 工作流程

整个流程分为 4 个阶段：

### 阶段 1：PDF 文本提取

按优先级依次尝试，每次尝试后检查结果完整性：

| 优先级 | 方法 | 说明 |
|--------|------|------|
| 1 | 内置 `pdf` 工具 | 最快，首选 |
| 2 | `pdftotext`（poppler） | `brew install poppler` |
| 3 | PyMuPDF | `pip3 install PyMuPDF` |
| 4 | 图片 OCR | 针对图片化页面，通过 PyMuPDF 导出 PNG 后 OCR |
| 兜底 | 用户协助 | 请求用户手动导出文本或截图 |

每个方法最多尝试一次，安装命令超时 120 秒即跳过，不反复重试。

### 阶段 2：翻译

- 将提取的英文文本保存为 `original.md`
- 逐章翻译为中文，保存为 `translated.md`
- 保留英文术语（如 MVP、PMF、CAC、LTV、GTM）
- 保持原文格式：加粗、列表、引用、表格
- 超过 20 页时分批翻译

### 阶段 3：排版生成

使用 `build_pdf.py` 脚本生成 HTML 和 PDF，包含：

- **封面页**：暖米色背景，书名 + 副标题 + 出处
- **目录页**：点状引导线连接章节名
- **章节页**：暖米色背景 + 金色竖线装饰
- **正文**：白色底色，2.8cm 页边距，A4 尺寸

### 阶段 4：PDF 导出

通过本地 HTTP Server 托管 HTML，使用浏览器工具导出为 PDF。

## 输出文件

| 文件 | 说明 |
|------|------|
| `original.md` | 原始英文文本 |
| `translated.md` | 中文翻译 |
| `<书名>.html` | 精排 HTML |
| `<书名>.pdf` | 最终 PDF 电子书 |

## 项目结构

```
pdf-to-ebook/
├── SKILL.md        # 工作流定义（触发条件、各阶段步骤）
├── build_pdf.py    # 排版脚本：Markdown → 精排 HTML
└── README.md       # 项目说明
```

## 使用方法

本项目作为 Claude Code Skill 使用，通过 SKILL.md 定义工作流。使用方式：

- 上传英文 PDF 并要求翻译成中文电子书
- 说"把这个 PDF 做成中文电子书"或"翻译这份 PDF 并排版"

## 排版效果

生成的电子书包含以下元素：

- A4 尺寸页面
- 暖米色封面（金色装饰线）
- 自动目录（点状引导线）
- 章节独立标题页
- 中文宋体正文（STSong / Songti SC）
- 代码块、表格、引用块样式
- 分页控制（封面/目录/章节页独立分页）

## 许可证

MIT
